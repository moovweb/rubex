You need to install oniguruma-5.9.2 with PREFIX=/usr/local

You also need go-gb

To build the package

   gb -b

To install the package

   gb -i

To run tests

   gb -t

To clean up 

   gb -c -m

To run benchmarks

   gotest -test.bench="."
